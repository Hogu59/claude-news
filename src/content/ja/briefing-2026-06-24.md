---
title: "Claude Code デイリーブリーフィング - 2026-06-24"
date: 2026-06-24
lang: ja
description: "昨夜（6/23）v2.1.187 が公開 — 2日連続の機能リリースです。ヘッドラインは2つ。①新設定 `sandbox.credentials` — サンドボックス内で動くコマンドが「クレデンシャルファイル」と「シークレット環境変数」を読めないように遮断します（エージェントが .env やトークンを吸い出して漏らす経路を、設定1行で塞ぐ）。②組織がモデルを制限できる — 管理者が許可モデルのリストを定義すると、ユーザーがモデルピッカー・`--model`・`/model`・`ANTHROPIC_MODEL` で制限対象モデルを選んだとき \"restricted by your organization's settings\" と表示されます（輸出規制でモデル可用性が揺れる今、チームを\"コンプライアントなモデル\"に固定するレバー）。ワークフロー：①`sandbox.credentials` で無人自動化のシークレット流出経路を塞ぐ — 6/24 のおすすめ記事「AIでGoogleをハックして$50万」が示すように、最も多い脆弱性は\"認証欠如のAPI・権限チェック漏れ\"であり、エージェントが触れるクレデンシャルそのものを減らすのが最も安い一次防御。②応答が止まったリモートMCPツール呼び出しが、5分の無限ハングではなく自動中断（エラー）になりました — `CLAUDE_CODE_MCP_TOOL_IDLE_TIMEOUT` で調整。死んだMCPサーバーにセッション全体が巻き込まれる事故を防ぐガード。セキュリティ：6/23 にもう一度インシデント — 14:19 UTC に複数モデルの elevated error rate を調査開始 → 14:25 に特定・修正、約16:44 UTC に解消（約2時間半）。Claude.ai・Console・API・Claude Code・Cowork が同時に影響、Downdetector はピークで8,000件超の報告。6/18 の全面障害・6/20 の'Opus 4.8 errors'に続く今週3度目の安定性のぐらつき — CI/cron はリトライ・バックオフ必須。さらに Fable 5・Mythos 5 は輸出規制12日目で依然として全ユーザーにオフライン、かつ6/23 でサブスク無料同梱期間も終了（6/19 の幹部の'数日で復帰'は未実現）。小さな変更：韓国語/CJKの貼り付けが mojibake で化けていた問題を修正（バイト単位の拡張キーで貼り付けを渡す端末）、`/install-github-app` がGitHub Actionsワークフロー設定を省いてアプリのみ導入可能に、フルスクリーンメニューのマウスクリック、サブエージェントのdepth・worktree掃除の修正など。記事：「AIでGoogleをハックして$50万」（brutecat — ClaudeをMCPエージェントとして接続し、1,500+のAPI・3,600のキーを3か月未満で自動 probe。鍵は'AI単独'ではなく、operation IDで検証を強制した構造）・「エージェンティック・テスティング」（Slack Engineering — 200+の実験、1回$15〜30と高価だが'UI経路'ではなく'目標達成'を検証、テストピラミッドの頂点に置け、Playwright MCP が単純0%・複雑0〜12%の失敗率）。プロジェクト：gh-orbit（複数worktreeのPR・CI・diffを1つのターミナルダッシュボードに）・CVERader（毎日押し寄せるCVE・セキュリティニュースを一か所に集めたブリーフィング）。"
---

# Claude Code デイリーブリーフィング - 2026-06-24

## 最新リリース概要

| バージョン | 日付 | 主な変更 |
|------|------|----------|
| **v2.1.187** | 6/23 | **`sandbox.credentials`**（サンドボックスのコマンドがクレデンシャルファイル・シークレットenvを読めないよう遮断）、**組織単位のモデル制限**（ピッカー・`--model`・`/model`・`ANTHROPIC_MODEL`）、フルスクリーンメニューの**マウスクリック**、**リモートMCPツール呼び出しの5分ハング → 自動中断**（`CLAUDE_CODE_MCP_TOOL_IDLE_TIMEOUT`）、**韓国語/CJK貼り付けのmojibake修正**、`/install-github-app` のワークフロー設定を任意化 など |
| v2.1.186 | 6/22 | `claude mcp login/logout <name>`、`!` bash 出力に Claude が自動応答（`respondToBashCommands`）、`CLAUDE_CODE_MAX_RETRIES` を15に上限化 + `RETRY_WATCHDOG`、background サブエージェントの権限プロンプトをメインに表示（6/23 で詳説） |

昨夜（6/23）**v2.1.187** が公開されました — v2.1.186（6/22）に続く**2日連続の機能リリース**です。ヘッドラインは、**サンドボックスのコマンドからクレデンシャル・シークレットの読み取りを遮断する `sandbox.credentials`** と、**組織が利用可能なモデルを制限する機能**です（下記の新機能）。本日のもう一つの軸は、**6/23 に再び発生したプラットフォームのインシデント**です — 今週3度目の安定性のぐらつきです（セキュリティ・制限事項）。

[リリースノート全文](https://github.com/anthropics/claude-code/releases)

---

## 主要な新機能と実践活用

### `sandbox.credentials` — サンドボックスのコマンドが「クレデンシャルファイル・シークレット環境変数」を読めないようにする (v2.1.187)

今回のリリースのヘッドラインです。新設定 **`sandbox.credentials`** は、**サンドボックス内で実行されるコマンド**が **クレデンシャルファイル**（例：`~/.aws/credentials`、`.env`、トークンファイル）と**シークレット環境変数**を読み取れないように遮断します。

```jsonc
// .claude/settings.json — サンドボックスのコマンドからクレデンシャル/シークレットへのアクセスを遮断
{
  "sandbox": {
    "credentials": "deny"   // 正確なキー/値の表記は公式 settings ドキュメントに従ってください
  }
}
```

実務上の意味は明確です — エージェントが動く環境には、ほぼ必ず **APIキー・クラウドのクレデンシャル・トークン**がどこかに置かれており、自動化で任意のシェルコマンドを回すと、その秘密が**コマンド出力・ログ・外部呼び出しを通じて漏れる**経路が生まれます。`sandbox.credentials` は、この経路を**モデルの善意ではなくランタイムの遮断**で塞ぎます — 6/22 で扱った「必ず守るべきルールはプロンプトではなく hook で強制する」と同じ筋です（ここでは hook ではなく**サンドボックス設定**がその役割を果たします）。6/24 のおすすめ記事「**AIでGoogle APIを突いて$50万のバグバウンティ**」がまさに対になります — そこで判明した最も多い脆弱性が**認証欠如のAPI・権限チェック漏れ**だった以上、**エージェントが触れられるクレデンシャル自体を減らすこと**が最も安い一次防御です。[GitHub v2.1.187](https://github.com/anthropics/claude-code/releases/tag/v2.1.187)

### 組織が「利用可能なモデル」を制限する — ピッカー・`--model`・`ANTHROPIC_MODEL` の全経路に適用 (v2.1.187)

管理者が **組織単位で許可モデルを定義できる**ようになりました。ユーザーが **モデルピッカー**・**`--model` フラグ**・**`/model` コマンド**・**`ANTHROPIC_MODEL` 環境変数** のいずれかの経路で **制限対象のモデル** を選ぶと、**"restricted by your organization's settings"** というメッセージが表示されます。

要点は、**すべての入口に一貫して**適用される点です — 誰かが `ANTHROPIC_MODEL` を環境変数で回避しても同じ制限を受けます。最近の流れと重ねると意味が際立ちます — **輸出規制でモデル可用性が揺れ**（下記セキュリティの Fable 5・Mythos 5）、**モデルが静かに deprecated・自動置換される**（6/20）状況で、組織が **「うちのチームはこのモデルだけ使う」** を中央で固定するレバーが手に入りました。CI やチーム標準で **コンプライアンス・コスト・再現性** のためにモデルを固定したいとき、これからは個々の開発者の設定ではなく **組織ポリシー** として強制できます。[GitHub v2.1.187](https://github.com/anthropics/claude-code/releases/tag/v2.1.187)

---

## 開発者ワークフローティップス

### 無人自動化の「シークレット流出経路」を `sandbox.credentials` で塞ぐ

上記の `sandbox.credentials` を最初に適用すべき場所は、**無人で動く自動化**です。CI・cron・background セッションでエージェントが任意のシェルコマンドを回すとき、そのコマンドはマシン上の**クレデンシャルファイルとシークレット環境変数にデフォルトでアクセス**できました — 意図の有無に関わらず、一度出力に載れば**ログ・成果物・外部呼び出しを通じて漏れ得ます**。

レシピは単純です — 自動化プロファイルの `.claude/settings.json` で **`sandbox.credentials` によりクレデンシャル・シークレットの読み取りを遮断**し、エージェントが本当に秘密を必要とする作業（例：特定のAPI呼び出し）は**その秘密をモデルに露出しないツール/MCPサーバーの背後に**押し込むことです。6/21 で扱った **WIF（静的キーの排除）**、6/19 の **サブエージェント最小権限（`tools:` を絞る）** と同じ位置にあります — 「**エージェントが触れられる秘密の表面積を減らす**」という一方向の三つ目の道具です。（正確なキー・値の表記は公式 settings ドキュメントに従ってください。）[GitHub v2.1.187](https://github.com/anthropics/claude-code/releases/tag/v2.1.187)

### 止まったリモートMCPツール呼び出しが「無限ハング」ではなく中断されるように — `CLAUDE_CODE_MCP_TOOL_IDLE_TIMEOUT`

CI・cron で Claude Code を無人実行するなら、この一行の修正を押さえてください — **応答がないまま5分間止まったリモートMCPツール呼び出し**が、**無限にブロックする代わりにエラーで自動中断**されるようになりました。しきい値は **`CLAUDE_CODE_MCP_TOOL_IDLE_TIMEOUT`** で調整できます。

```bash
# 死んだリモートMCPサーバーにセッション全体が巻き込まれるのを防ぐ（例：60秒のidleで中断）
export CLAUDE_CODE_MCP_TOOL_IDLE_TIMEOUT=60
```

要点は、**一つのハングが無人実行全体を止める事故を防ぐ**ガードだという点です。リモートMCPサーバーがネットワーク問題で応答を絶つと、これまではツール呼び出しがその場で止まり、**セッション全体がハングに陥り得ました**。今後は idle タイムアウトがその呼び出しを断ち切り、エージェントが**次のステップに進むか、きれいに失敗**できるようにします — cron パイプラインで「なぜ昨夜のジョブが朝まで止まっていたのか」を減らす類の変更です。長い無人ジョブを回すなら、この変数を**ワークロードに合わせて短めに**設定しておきましょう。[GitHub v2.1.187](https://github.com/anthropics/claude-code/releases/tag/v2.1.187)

---

## セキュリティ・制限事項

### 6/23 に再びインシデント — 複数モデルの elevated error rate（今週3度目）

6/23、**Claude 全般で再びインシデント**が発生しました。status.claude.com の記録によれば、**14:19 UTC に複数モデルの elevated error rate の調査を開始**し、**14:25 UTC に原因を特定・修正を適用**、そして**約16:44 UTC に解消**しました（約2時間半）。影響範囲は広く、**Claude.ai・Claude Console・Claude API・Claude Code・Claude Cowork** が同時に影響を受け、Downdetector にはピークで**米国だけで8,000件超の報告**が寄せられました。

個々のインシデントは復旧しましたが、**パターンこそがシグナル**です — 6/18 の全面障害、6/20 の「Opus 4.8 errors」に続き、**今週だけで3度目**の安定性のぐらつきです。ターミナルの Claude Code も同じバックエンドに繋がっているため、短い障害でも無人作業が丸ごと壊れ得ます。CI・cron で Claude Code を回すなら、**リトライ・バックオフと失敗通知**をパイプラインに組み込むのが変わらぬ実務対応です（6/22 で扱った `CLAUDE_CODE_MAX_RETRIES` の上限15・`RETRY_WATCHDOG` と併せて点検してください）。[Claude Status — Incident History](https://status.claude.com/history) · [TechRadar](https://www.techradar.com/news/live/claude-down-june-23-2026)

### Fable 5・Mythos 5 — 輸出規制12日目、依然として全ユーザーにオフライン + サブスク同梱も終了 (6/23)

複数日にわたる話題なので簡潔に。**6/23 で2つのことが重なりました。** ①**輸出規制12日目**（6/12 の命令）、**Fable 5・Mythos 5 は依然として全ユーザーにオフライン**です — 外国籍だけでなく、リアルタイムで米国人/外国籍を分ける方法がないため、**世界中の全員**に対して停止が続いています。6/19 に幹部が自信を見せた **「数日で復帰」は、6/24 時点でまだ実現していません。** ②同時に、**6/23 で Fable 5 のサブスク無料同梱期間（6/9〜6/22）が終了**しました — つまりモデルが戻っても、無料バンドルではなく**別課金（usage credit）**の対象になります。

要するに、**「いつ戻るか」と「戻ったらどう課金されるか」の両方が不利な方向に**固まりました。外国籍の開発者（韓国・日本など）はそもそもアクセスが断たれているため、実務対応は変わりません — `fallbackModel`・`availableModels` で **Opus 4.8 クラスの代替経路**を用意し、モデル可用性が会社ではなく**地政学と政策に左右され得る**という前提で自動化を設計してください。[Anthropic 声明](https://www.anthropic.com/news/fable-mythos-access) · [cryptobriefing](https://cryptobriefing.com/anthropic-fable-5-us-export-controls/)

---

## 知っておくと便利な小さな変更点

v2.1.187 で上記に収まらなかった実用的な変更・修正です。

- **韓国語/CJK貼り付けの mojibake 修正**: 貼り付けを**バイト単位の拡張キーイベントとして渡す端末**で、**韓国語・CJKテキストが化けて入力されていた**問題が修正されました — 韓国・日本のユーザーに直接届く修正です (v2.1.187)
- **`/install-github-app` のワークフロー設定を任意化**: これからは **GitHub App だけを導入**し、GitHub Actions のワークフロー・シークレット設定の手順を**スキップ**できます (v2.1.187)
- **フルスクリーンメニューでマウスクリック**: 権限プロンプト・`/model`・`/config` のような選択メニューを、フルスクリーンモードで**マウスでクリック**して選べます (v2.1.187)
- **サブエージェントの depth 追跡修正**: 再開（resumed）されたサブエージェントが**元のスポーン depth を復元**し、fork されたサブエージェントが **depth 上限にカウント**されるよう修正。また、死んだエージェントが残した**ロック済みの `.git/worktrees/` 登録を自動で掃除**します (v2.1.187)
- **`--resume` の "No conversation found" 修正**: 元の `-p` 実行が**モデルのターンを1つも生成しなかった**場合に `--resume` が失敗していた問題が修正されました (v2.1.187)
- **構造化出力の無限再呼び出し修正**: `--json-schema`・workflow `agent({schema})` で、モデルが成功後も **`StructuredOutput` を無限に再呼び出し**していた問題が修正され、background ジョブが構造化出力なしでターンを終えたときに **"working" のまま固まる**問題も修正されました (v2.1.187)
- **`/plugin` の整理ナッジ + `/btw` ナビ**: `/plugin` が**最近使っていないプラグインを表示**して整理を促し、`/btw` は **←/→ 矢印で以前の回答を辿れる**ようになりました (v2.1.187)

---

## おすすめコラム＆読み物

- **「AIでGoogleをハックして$50万を稼ぐ」（brutecat）**: セキュリティ研究者が **Claude を MCP エージェントとして接続**し、Google の API を自動で突いた実戦記録です。規模が印象的です — Android APK・トラフィック傍受・バイナリ解析で集めた **3,600 個の API キー**で **1,500 以上の API**を **3か月未満**で自動 probe し、**$50万 のバグバウンティ**を稼ぎました。要点は2つ。①発見された脆弱性の大半は精巧なエクスプロイトではなく、**認証欠如のAPI・権限チェック漏れ**といった*基本的なミス*でした — AI は**巨大な攻撃面に同じテストを粘り強く反復する**のが得意です。②しかし効果は「AI 単独」ではなく**構造から**生まれました — API を丸ごと投げず**論理的なグループに束ねて**コンテキストを共有させ、レポートに **probe 応答の operation ID を強制的に含めさせて**、未検証のヒットを*検証可能な*発見に変えたのです。上記 `sandbox.credentials` とちょうど対になる、「エージェントで攻撃面を洗いつつ検証は人がロックする」実戦レシピです。[brutecat](https://brutecat.com/articles/hacking-google-with-ai/)
- **「エージェンティック・テスティング — E2Eテストスタックにおけるエージェントの役割」（Slack Engineering）**: Slack のエンジニアリングチームが **200 を超えるエージェンティック・ワークフローの実験**で、*エージェントベースのテスト*が従来の決定論的テストとどこで分かれるかを計測した記事です。結論はバランスが取れています — エージェント実行は **1回 $15〜30** と従来テストよりはるかに高価で遅いものの、**特定の UI 経路を強制**する代わりに**目標が達成されたかを検証**する点で性質が異なります。そのため **複雑な UI 挙動の探索・不安定（flaky）なワークフローのデバッグ・本番バグの再現**に強く、**テストピラミッドの頂点**に置いて既存の E2E テストを*置き換え*ではなく*補完*せよと勧めます。興味深い実測 — **Playwright MCP** が単純シナリオ **0%**、複雑ワークフロー **0〜12%** の低い失敗率で他のアプローチを上回りました。エージェントにテストを任せる際、「何を、どこに置けばコスト対効果が出るか」のデータドリブンな指針です。[Slack Engineering](https://slack.engineering/agentic-testing-where-agents-fit-in-the-e2e-testing-stack/)

---

## 注目プロジェクト＆ツール

- **gh-orbit — 複数worktreeのPR・CI・diffを「1つのターミナルダッシュボード」に**: 複数の Git worktree を**1つのターミナルダッシュボードにまとめる** `gh`（GitHub CLI）拡張です。各ブランチの**オープンな PR と CI 状態を一目で**見せ、**構文ハイライト付きの diff** をその場で確認し、**PR のマージまでターミナルで**こなします。要点は、**ターミナル ↔ git コマンド ↔ ブラウザタブ**を絶えず行き来する摩擦をなくすこと — 複数の worktree を並列で回す流れに最適化されています。6/20 で扱った auto モード・worktree 分離、そして「AI は feature branch だけ push、マージは人が PR で」というゲートとちょうど対になります — **エージェントを worktree ごとに並列で回す人**が、その成果（PR・CI）を1画面で点検するのに実用的です。[GitHub](https://github.com/jeonbyeongmin/gh-orbit)
- **CVERader — 毎日押し寄せるCVE・セキュリティニュースを一か所に集めたブリーフィング**: 複数のソースに散らばる **CVE 情報とセキュリティニュースを1つのプラットフォームに**まとめるサービスです。セキュリティサイトを一つひとつ巡らなくても、**今すぐ対処すべき致命的な脆弱性**を素早く見分けられるよう、**日次のセキュリティブリーフィング・週次レポート・CVSS スコアに基づく CVE モニタリング**を提供します。上記「AIでGoogleをハック」の事例が示すとおり、脆弱性の大半が*基本的なミス*である以上、「自分が使う依存関係に今日何が起きたか」を毎日まとめて確認したい開発者に実用的です。[CVERader](https://cverader.com)
